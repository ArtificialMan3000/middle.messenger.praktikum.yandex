<div class="{{css.field}} {{className}}">
  <label for="{{id}}" class="{{css.label}} sr-only">{{label}}</label>
  <input class="{{css.input}}" id="{{id}}" type="{{type}}" placeholder="{{placeholder}}">
</div>